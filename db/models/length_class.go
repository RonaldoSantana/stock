// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
)

// LengthClass is an object representing the database table.
type LengthClass struct {
	ID    int     `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name  string  `boil:"name" json:"name" toml:"name" yaml:"name"`
	Unit  string  `boil:"unit" json:"unit" toml:"unit" yaml:"unit"`
	Value float64 `boil:"value" json:"value" toml:"value" yaml:"value"`

	R *lengthClassR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lengthClassL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// lengthClassR is where relationships are stored.
type lengthClassR struct {
	Products ProductSlice
}

// lengthClassL is where Load methods for each relationship are stored.
type lengthClassL struct{}

var (
	lengthClassColumns               = []string{"id", "name", "unit", "value"}
	lengthClassColumnsWithoutDefault = []string{"name", "unit", "value"}
	lengthClassColumnsWithDefault    = []string{"id"}
	lengthClassPrimaryKeyColumns     = []string{"id"}
)

type (
	// LengthClassSlice is an alias for a slice of pointers to LengthClass.
	// This should generally be used opposed to []LengthClass.
	LengthClassSlice []*LengthClass
	// LengthClassHook is the signature for custom LengthClass hook methods
	LengthClassHook func(boil.Executor, *LengthClass) error

	lengthClassQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lengthClassType                 = reflect.TypeOf(&LengthClass{})
	lengthClassMapping              = queries.MakeStructMapping(lengthClassType)
	lengthClassPrimaryKeyMapping, _ = queries.BindMapping(lengthClassType, lengthClassMapping, lengthClassPrimaryKeyColumns)
	lengthClassInsertCacheMut       sync.RWMutex
	lengthClassInsertCache          = make(map[string]insertCache)
	lengthClassUpdateCacheMut       sync.RWMutex
	lengthClassUpdateCache          = make(map[string]updateCache)
	lengthClassUpsertCacheMut       sync.RWMutex
	lengthClassUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var lengthClassBeforeInsertHooks []LengthClassHook
var lengthClassBeforeUpdateHooks []LengthClassHook
var lengthClassBeforeDeleteHooks []LengthClassHook
var lengthClassBeforeUpsertHooks []LengthClassHook

var lengthClassAfterInsertHooks []LengthClassHook
var lengthClassAfterSelectHooks []LengthClassHook
var lengthClassAfterUpdateHooks []LengthClassHook
var lengthClassAfterDeleteHooks []LengthClassHook
var lengthClassAfterUpsertHooks []LengthClassHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *LengthClass) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *LengthClass) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *LengthClass) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *LengthClass) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *LengthClass) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *LengthClass) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *LengthClass) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *LengthClass) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *LengthClass) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range lengthClassAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddLengthClassHook registers your hook function for all future operations.
func AddLengthClassHook(hookPoint boil.HookPoint, lengthClassHook LengthClassHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		lengthClassBeforeInsertHooks = append(lengthClassBeforeInsertHooks, lengthClassHook)
	case boil.BeforeUpdateHook:
		lengthClassBeforeUpdateHooks = append(lengthClassBeforeUpdateHooks, lengthClassHook)
	case boil.BeforeDeleteHook:
		lengthClassBeforeDeleteHooks = append(lengthClassBeforeDeleteHooks, lengthClassHook)
	case boil.BeforeUpsertHook:
		lengthClassBeforeUpsertHooks = append(lengthClassBeforeUpsertHooks, lengthClassHook)
	case boil.AfterInsertHook:
		lengthClassAfterInsertHooks = append(lengthClassAfterInsertHooks, lengthClassHook)
	case boil.AfterSelectHook:
		lengthClassAfterSelectHooks = append(lengthClassAfterSelectHooks, lengthClassHook)
	case boil.AfterUpdateHook:
		lengthClassAfterUpdateHooks = append(lengthClassAfterUpdateHooks, lengthClassHook)
	case boil.AfterDeleteHook:
		lengthClassAfterDeleteHooks = append(lengthClassAfterDeleteHooks, lengthClassHook)
	case boil.AfterUpsertHook:
		lengthClassAfterUpsertHooks = append(lengthClassAfterUpsertHooks, lengthClassHook)
	}
}

// OneP returns a single lengthClass record from the query, and panics on error.
func (q lengthClassQuery) OneP() *LengthClass {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single lengthClass record from the query.
func (q lengthClassQuery) One() (*LengthClass, error) {
	o := &LengthClass{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for length_class")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all LengthClass records from the query, and panics on error.
func (q lengthClassQuery) AllP() LengthClassSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all LengthClass records from the query.
func (q lengthClassQuery) All() (LengthClassSlice, error) {
	var o []*LengthClass

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to LengthClass slice")
	}

	if len(lengthClassAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all LengthClass records in the query, and panics on error.
func (q lengthClassQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all LengthClass records in the query.
func (q lengthClassQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count length_class rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q lengthClassQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q lengthClassQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if length_class exists")
	}

	return count > 0, nil
}

// ProductsG retrieves all the product's product.
func (o *LengthClass) ProductsG(mods ...qm.QueryMod) productQuery {
	return o.Products(boil.GetDB(), mods...)
}

// Products retrieves all the product's product with an executor.
func (o *LengthClass) Products(exec boil.Executor, mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Select("`a`.*"),
	}

	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`a`.`length_class_id`=?", o.ID),
	)

	query := Products(exec, queryMods...)
	queries.SetFrom(query.Query, "`product` as `a`")
	return query
}

// LoadProducts allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (lengthClassL) LoadProducts(e boil.Executor, singular bool, maybeLengthClass interface{}) error {
	var slice []*LengthClass
	var object *LengthClass

	count := 1
	if singular {
		object = maybeLengthClass.(*LengthClass)
	} else {
		slice = *maybeLengthClass.(*[]*LengthClass)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &lengthClassR{}
		}
		args[0] = object.ID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &lengthClassR{}
			}
			args[i] = obj.ID
		}
	}

	query := fmt.Sprintf(
		"select * from `product` where `length_class_id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)
	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load product")
	}
	defer results.Close()

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice product")
	}

	if len(productAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Products = resultSlice
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.LengthClassID {
				local.R.Products = append(local.R.Products, foreign)
				break
			}
		}
	}

	return nil
}

// AddProductsG adds the given related objects to the existing relationships
// of the length_class, optionally inserting them as new records.
// Appends related to o.R.Products.
// Sets related.R.LengthClass appropriately.
// Uses the global database handle.
func (o *LengthClass) AddProductsG(insert bool, related ...*Product) error {
	return o.AddProducts(boil.GetDB(), insert, related...)
}

// AddProductsP adds the given related objects to the existing relationships
// of the length_class, optionally inserting them as new records.
// Appends related to o.R.Products.
// Sets related.R.LengthClass appropriately.
// Panics on error.
func (o *LengthClass) AddProductsP(exec boil.Executor, insert bool, related ...*Product) {
	if err := o.AddProducts(exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddProductsGP adds the given related objects to the existing relationships
// of the length_class, optionally inserting them as new records.
// Appends related to o.R.Products.
// Sets related.R.LengthClass appropriately.
// Uses the global database handle and panics on error.
func (o *LengthClass) AddProductsGP(insert bool, related ...*Product) {
	if err := o.AddProducts(boil.GetDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddProducts adds the given related objects to the existing relationships
// of the length_class, optionally inserting them as new records.
// Appends related to o.R.Products.
// Sets related.R.LengthClass appropriately.
func (o *LengthClass) AddProducts(exec boil.Executor, insert bool, related ...*Product) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.LengthClassID = o.ID
			if err = rel.Insert(exec); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `product` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"length_class_id"}),
				strmangle.WhereClause("`", "`", 0, productPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.LengthClassID = o.ID
		}
	}

	if o.R == nil {
		o.R = &lengthClassR{
			Products: related,
		}
	} else {
		o.R.Products = append(o.R.Products, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &productR{
				LengthClass: o,
			}
		} else {
			rel.R.LengthClass = o
		}
	}
	return nil
}

// LengthClassesG retrieves all records.
func LengthClassesG(mods ...qm.QueryMod) lengthClassQuery {
	return LengthClasses(boil.GetDB(), mods...)
}

// LengthClasses retrieves all the records using an executor.
func LengthClasses(exec boil.Executor, mods ...qm.QueryMod) lengthClassQuery {
	mods = append(mods, qm.From("`length_class`"))
	return lengthClassQuery{NewQuery(exec, mods...)}
}

// FindLengthClassG retrieves a single record by ID.
func FindLengthClassG(id int, selectCols ...string) (*LengthClass, error) {
	return FindLengthClass(boil.GetDB(), id, selectCols...)
}

// FindLengthClassGP retrieves a single record by ID, and panics on error.
func FindLengthClassGP(id int, selectCols ...string) *LengthClass {
	retobj, err := FindLengthClass(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindLengthClass retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLengthClass(exec boil.Executor, id int, selectCols ...string) (*LengthClass, error) {
	lengthClassObj := &LengthClass{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `length_class` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(lengthClassObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from length_class")
	}

	return lengthClassObj, nil
}

// FindLengthClassP retrieves a single record by ID with an executor, and panics on error.
func FindLengthClassP(exec boil.Executor, id int, selectCols ...string) *LengthClass {
	retobj, err := FindLengthClass(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *LengthClass) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *LengthClass) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *LengthClass) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *LengthClass) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no length_class provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lengthClassColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	lengthClassInsertCacheMut.RLock()
	cache, cached := lengthClassInsertCache[key]
	lengthClassInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			lengthClassColumns,
			lengthClassColumnsWithDefault,
			lengthClassColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(lengthClassType, lengthClassMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lengthClassType, lengthClassMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `length_class` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `length_class` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `length_class` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, lengthClassPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into length_class")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == lengthClassMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for length_class")
	}

CacheNoHooks:
	if !cached {
		lengthClassInsertCacheMut.Lock()
		lengthClassInsertCache[key] = cache
		lengthClassInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single LengthClass record. See Update for
// whitelist behavior description.
func (o *LengthClass) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single LengthClass record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *LengthClass) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the LengthClass, and panics on error.
// See Update for whitelist behavior description.
func (o *LengthClass) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the LengthClass.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *LengthClass) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	lengthClassUpdateCacheMut.RLock()
	cache, cached := lengthClassUpdateCache[key]
	lengthClassUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			lengthClassColumns,
			lengthClassPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update length_class, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `length_class` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, lengthClassPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lengthClassType, lengthClassMapping, append(wl, lengthClassPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update length_class row")
	}

	if !cached {
		lengthClassUpdateCacheMut.Lock()
		lengthClassUpdateCache[key] = cache
		lengthClassUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q lengthClassQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q lengthClassQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for length_class")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o LengthClassSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o LengthClassSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o LengthClassSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LengthClassSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lengthClassPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `length_class` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lengthClassPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in lengthClass slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *LengthClass) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *LengthClass) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *LengthClass) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *LengthClass) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no length_class provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lengthClassColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lengthClassUpsertCacheMut.RLock()
	cache, cached := lengthClassUpsertCache[key]
	lengthClassUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			lengthClassColumns,
			lengthClassColumnsWithDefault,
			lengthClassColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			lengthClassColumns,
			lengthClassPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert length_class, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "length_class", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `length_class` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(lengthClassType, lengthClassMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lengthClassType, lengthClassMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for length_class")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == lengthClassMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for length_class")
	}

CacheNoHooks:
	if !cached {
		lengthClassUpsertCacheMut.Lock()
		lengthClassUpsertCache[key] = cache
		lengthClassUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single LengthClass record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *LengthClass) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single LengthClass record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *LengthClass) DeleteG() error {
	if o == nil {
		return errors.New("models: no LengthClass provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single LengthClass record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *LengthClass) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single LengthClass record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LengthClass) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no LengthClass provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lengthClassPrimaryKeyMapping)
	sql := "DELETE FROM `length_class` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from length_class")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q lengthClassQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q lengthClassQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no lengthClassQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from length_class")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o LengthClassSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o LengthClassSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no LengthClass slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o LengthClassSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LengthClassSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no LengthClass slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(lengthClassBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lengthClassPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `length_class` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lengthClassPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from lengthClass slice")
	}

	if len(lengthClassAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *LengthClass) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *LengthClass) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *LengthClass) ReloadG() error {
	if o == nil {
		return errors.New("models: no LengthClass provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LengthClass) Reload(exec boil.Executor) error {
	ret, err := FindLengthClass(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *LengthClassSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *LengthClassSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LengthClassSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty LengthClassSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LengthClassSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	lengthClasses := LengthClassSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lengthClassPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `length_class`.* FROM `length_class` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, lengthClassPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&lengthClasses)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in LengthClassSlice")
	}

	*o = lengthClasses

	return nil
}

// LengthClassExists checks if the LengthClass row exists.
func LengthClassExists(exec boil.Executor, id int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `length_class` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if length_class exists")
	}

	return exists, nil
}

// LengthClassExistsG checks if the LengthClass row exists.
func LengthClassExistsG(id int) (bool, error) {
	return LengthClassExists(boil.GetDB(), id)
}

// LengthClassExistsGP checks if the LengthClass row exists. Panics on error.
func LengthClassExistsGP(id int) bool {
	e, err := LengthClassExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// LengthClassExistsP checks if the LengthClass row exists. Panics on error.
func LengthClassExistsP(exec boil.Executor, id int) bool {
	e, err := LengthClassExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
